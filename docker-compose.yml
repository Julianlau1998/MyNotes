version: "3.7"
services:
  mynotes:
    image: mynotes
    container_name: mynotes
    build:
      context: ./MyNotes
    ports:
      - 8080:8080
    restart: on-failure
  
  backend:
    image: mynotes-backend
    container_name: mynotes_backend
    build:
      context: ./MyNotes_backend
    ports:
      - 1324:1324
    restart: on-failure